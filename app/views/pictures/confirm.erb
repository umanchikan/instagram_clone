<h1>確認画面</h1>
<%= form_with(model: @picture, url: pictures_path, local: true) do |form| %>
  <!-- 確認画面でアップロードした画像を表示させるための記述 -->
  <%= form.hidden_field :content %>
  　<%= @picture.content %>
  <%= image_tag(@picture.blog_picture.url) if @picture.blog_picture && @picture.blog_picture.url %>
  <%= form.hidden_field :blog_picture_cache %>
  <%= form.submit "投稿する" %>
<% end %>
<%= form_with(model:@picture, url:new_picture_path, local:true, method:"get") do |form| %>
  <%= form.hidden_field :blog_picture_cache %>
  <%= form.submit "戻る", name: "back" %>
<% end %>